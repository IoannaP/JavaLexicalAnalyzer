/**
 * Enum containing the different types of java tokens
 */
public enum TokenType {
  INTEGER_LITERAL,
  FLOATING_POINT_LITERAL,
  IDENTIFIER,
  CHARACTER_LITERAL,
  STRING_LITERAL,
  SEPARATOR,
  OPERATOR,
  KEYWORD,
  NULL_LITERAL,
  BOOLEAN_LITERAL
}
